# Automating homelab infrastructure with terraform

## Requirement 
Install terraform from [https://developer.hashicorp.com/terraform/install](https://developer.hashicorp.com/terraform/install)

## Guide
``` shell
cp credentials.example credentials.auto.tfvars
```

Then edit `credentials.auto.tfvars` file with your proxmox credential